(conteúdo disponível na aula 97)

# laço for each
É usado quando quer acessar cada item de uma estrutura, um por um, sem se preocupar com índices.

### Sintaxe:

```
for (TipoElementos nomeVariavel : colecao) {
    comandos
}
```
